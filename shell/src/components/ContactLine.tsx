/** Constant muted contact line on the entry/error screens (no contact CTA — DL-058/DL-062). */
export function ContactLine() {
  return (
    <p className="t-body-sm text-muted">
      Bei Problemen wende dich an deine Ansprechperson in der Organisation.
    </p>
  )
}
